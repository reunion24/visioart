import { useEffect } from 'react';
export const useClient = () => {
    useEffect(() => {
      // Place your custom logic here
      // Perform any client-side specific operations, initialize libraries, or manage client-specific state
    }, []);
  };
  